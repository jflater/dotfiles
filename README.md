# dotfiles

Curated config files (no secrets). Flat copy mirroring `$HOME` layout.

Included: `.bashrc`, `.bash_aliases`, `.profile`, `.tmux.conf`, `.gitconfig`,
`.config/{nvim,sqlfluff,starship.toml,pip}`.

Copy a file back with e.g. `cp .config/nvim/... ~/.config/nvim/...`.
